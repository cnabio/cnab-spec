{
  "$id": "https://cnab.io/v2/claim-result.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "output": {
      "description": "A value that is produced by running an invocation image",
      "type": "object",
      "properties": {
        "contentDigest": {
          "description": "A cryptographic hash digest of the contents of the output that can be used to validate the output",
          "type": "string"
        }
      },
      "additionalProperties": true
    }
  },
  "properties": {
    "additionalProperties": false,
    "claimId": {
      "description": "The claim ID",
      "type": "string"
    },
    "id": {
      "description": "The ID (a ULID)",
      "type": "string"
    },
    "labels": {
      "description": "Labels are a set of key/value pairs of type string that MAY be used for querying.",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "message": {
      "description": "The last message from the invocation image or runtime",
      "type": "string"
    },
    "namespace": {
      "description": "The namespace of the installation",
      "type": "string"
    },
    "created": {
      "description": "The date created, as an ISO-8601 Extended Format date string, as specified in the ECMAScript standard",
      "type": "string"
    },
    "custom": {
      "$comment": "reserved for custom extensions"
    },
    "outputs": {
      "additionalProperties": {
        "$ref": "#/definitions/output"
      },
      "description": "Map of outputs generated by the operation.",
      "type": "object"
    },
    "schemaVersion": {
      "description": "The version of the CNAB Installation State specification",
      "type": "string"
    },
    "status": {
      "description": "The status of the operation",
      "enum": [
        "cancelled",
        "failed",
        "succeeded",
        "pending",
        "running",
        "unknown"
      ],
      "type": "string"
    }
  },
  "required": [
    "claimId",
    "id",
    "created",
    "schemaVersion",
    "status"
  ],
  "title": "CNAB Claim Result json schema",
  "type": "object"
}
