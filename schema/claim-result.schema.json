{
  "$id": "https://cnab.io/v1/claim-result.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "claimRef": {
      "description": "A reference to the claim that generated this result",
      "properties": {
        "installation": {
          "description": "The claim installation name",
          "type": "string"
        },
        "revision": {
          "description": "The claim revision ID (a ULID)",
          "type": "string"
        }
      },
      "type":"object"
    },
    "output": {
      "description": "A value that is produced by running an invocation image",
      "type": "object"
    }
  },
  "properties": {
    "additionalProperties": false,
    "claimRef": {
      "$ref": "#/definitions/claimRef"
    },
    "id": {
      "description": "The ID (a ULID)",
      "type": "string"
    },
    "message": {
      "description": "The last message from the invocation image or runtime",
      "type": "string"
    },
    "modified": {
      "description": "The date last modified, as an ISO-8601 Extended Format date string, as specified in the ECMAScript standard",
      "type": "string"
    },
    "outputs": {
      "additionalProperties": {
        "$ref": "#/definitions/output"
      },
      "description": "Map of outputs generated by the operation.",
      "type": "object"
    },
    "started": {
      "description": "The date when execution of the operation started, as an ISO-8601 Extended Format date string, as specified in the ECMAScript standard",
      "type": "string"
    },
    "status": {
      "description": "The status of the operation",
      "enum": [
        "cancelled",
        "failed",
        "succeeded",
        "pending",
        "running",
        "unknown"
      ],
      "type": "string"
    },
    "stopped": {
      "description": "The date when execution of the operation stopped, as an ISO-8601 Extended Format date string, as specified in the ECMAScript standard",
      "type": "string"
    }
  },
  "required": [
    "claimRef",
    "id",
    "message",
    "modified",
    "status"
  ],
  "title": "CNAB Claim Result json schema",
  "type": "object"
}
